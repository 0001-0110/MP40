@model MP40.BLL.Models.Page;

<div>
	<label>
		Pages:
	</label>
	<div class="btn-group">
		@for (int i = 1; i <= Model.PageCount; i++)
		{
			string buttonClass = Model.PageIndex == i ? "btn-primary" : "btn-outline-primary";
			<a asp-action="Index" data-page="@i" class="pager-btn btn @buttonClass">@i</a>
		}
	</div>
</div>
